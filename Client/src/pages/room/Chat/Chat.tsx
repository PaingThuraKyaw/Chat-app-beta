
const Chat = () => {

  return (
    <div className=" bg-gray-100  h-screen">
      {/* <p className=" bg-black/50">{state?.username}</p>
      <p>{state?.message}</p> */}
    </div>
  );
};

export default Chat;
